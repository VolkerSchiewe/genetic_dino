import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/app';

// fill the div with the id "app" with the content of the App-Component
ReactDOM.render(
    <App/>,
    document.getElementById('app')
);